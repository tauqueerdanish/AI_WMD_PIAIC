"use strict";
let animalsList = ['cat', 'dog', 'parrot'];
for (let i = 0; i < animalsList.length; i++) {
    console.log(`${animalsList[i]} is a pet animal.`);
}
console.log('All of these animals are pet animals.\n We can kept them in our homes.\n and play with them.');
